# Phase 1 skeleton — pybind11 bindings added in Phase 3b
# find_package(pybind11 REQUIRED)
# pybind11_add_module(dsp_core python/dsp_core_module.cpp)
# target_link_libraries(dsp_core PRIVATE DspCore::DspCore)
